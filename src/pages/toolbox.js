import React from 'react';
import Layout from "../components/layout"

const ToolboxPage = () => {
    return (
        <Layout>
            <h1 className="text-3xl font-bold">Herramientas</h1>
            <p>
                Este es el contenido de la página de Herramientas.
            </p>
        </Layout>
    );
};

export default ToolboxPage;